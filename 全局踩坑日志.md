# 全局踩坑日志

用途：记录神驴和用户沟通、执行、检查电脑、整理文件、做页面、做文档时出现的分歧、误判、风险和改进办法。

原则：
- 这里记录的是长期经验，不是责备。
- 每次出现“我以为”和“用户实际想要”不一致时，都要补一条。
- 每次出现文件、权限、同步、账号、删除、授权、付款、API Key 相关问题时，都要补一条。
- 先记录事实，再记录原因，再记录以后怎么避免。

## 记录格式

日期：
任务：
分歧或踩坑：
确认到的事实：
原因判断：
以后规则：
状态：

## 已记录事项

### 2026-06-07 - 回收站是否为空

分歧或踩坑：
第一次用普通系统接口检查时，显示回收站为空；用户指出“没有空呀”。

确认到的事实：
后来从 `C:\$Recycle.Bin` 底层目录检查，确实存在回收站内容，包括安装包、照片、OneDrive 相关旧记录。

原因判断：
普通系统接口可能没有读到 OneDrive/当前用户/底层回收站项目。

以后规则：
涉及回收站时，不能只看一个接口。必须分两步：
1. 先看 Windows 回收站显示结果。
2. 再看底层 `$Recycle.Bin` 目录。

状态：
已修正规则。回收站未清空。

### 2026-06-07 - 文件能打开但 OneDrive 打红叉

分歧或踩坑：
用户看到桌面文件和文件夹有红叉，担心文件坏了或被删除；检查发现文件本地能打开。

确认到的事实：
`铁胆神驴Codex`、`神驴页面`、`桌面整理_2026-06-07` 都存在，文件能打开；红叉是 OneDrive 同步错误，不等于本地文件损坏。

原因判断：
OneDrive 同步层有问题，可能是账号、网络、文件名、权限、路径太深、特殊符号或同步冲突。

以后规则：
看到 OneDrive 红叉时，先判断“本地文件是否能打开”和“OneDrive 是否同步成功”是两件事。不能把红叉直接解释成文件损坏。

状态：
待用户提供 OneDrive 同步错误窗口截图。

### 2026-06-07 - 授权地址不能用

分歧或踩坑：
用户说授权地址里的文件都用不了。

确认到的事实：
文件都存在；旧的 Codex 授权链接可能是一性登录链接，已经过期；docx 是资料，不是直接授权入口。

原因判断：
授权、API Key、付款方式属于敏感区，不能自动代替用户完成登录、付款、复制 Key。

以后规则：
授权区必须分清楚：
- 官网入口
- 旧授权链接
- API Key 文档
- 付款资料
- 换 Key 脚本

状态：
已新增 OpenAI、Google AI Studio、Groq 的稳定入口和说明。

### 2026-06-07 - 用户授权与系统权限不一致

分歧或踩坑：
用户说已经把电脑权限给神驴，但实际 Codex 环境仍会要求路径读写授权。

确认到的事实：
即使用户口头授权，系统沙盒仍限制部分路径，需要按路径请求权限。

原因判断：
这是 Codex 安全机制，不是用户不信任，也不是神驴故意重复。

以后规则：
每次需要访问新路径时，简短说明“这是系统安全限制”，不要长篇解释。

状态：
持续遵守。

### 2026-06-07 - 删除和清理不能直接做

分歧或踩坑：
用户多次要求清理无用文件、彻底删除文件夹。

确认到的事实：
删除、覆盖、清空回收站属于高风险动作，必须先确认；部分删除命令也会被系统沙盒拒绝。

原因判断：
清理电脑容易误删资料，尤其 OneDrive 同步未稳定时风险更高。

以后规则：
清理分三步：
1. 先分出“建议删除清单”。
2. 用户确认。
3. 再执行或给用户手动删除指令。

状态：
持续遵守。

