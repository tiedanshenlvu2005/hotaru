# Codex 使用效率清单

这份清单是照着做的，不是摆着看的。目标只有一个：减少切换、减少重复判断、减少入口混乱。

## 1. 只认一个主首页

先打开 `文件总览.html`，不要从多个文件夹里乱跳。
主首页负责导航，其他文件负责内容。

## 2. 永远先读顺序

顺序固定为：

1. `Codex每日执行方针.txt`
2. `AGENTS.md`
3. `PRD.md`
4. `分类索引.md`
5. `执行顺序.md`

## 3. 规则、目标、动作分开看

- `Codex每日执行方针.txt`：怎么用
- `AGENTS.md`：怎么做
- `PRD.md`：做什么
- `执行顺序.md`：今天先走哪一步

## 4. 页面只负责展示，桌面只负责入口

- `神驴页面` 负责页面展示
- `铁胆神驴Codex` 负责文件整理和规则入口
- 不要把同一类入口放太多处

## 5. 只给关键文件做英文备份

优先保留这些文件的英文版：

- `README`
- `PRD`
- `AGENTS`
- `分类索引`

不要把所有文件都复制一遍，避免维护成本变高。

## 你现在就照着做

1. 双击 `shenlu-codex-entry.lnk`
2. 打开 `文件总览.html`
3. 先看 `Codex每日执行方针.txt`
4. 再看 `AGENTS.md`
5. 再看 `执行顺序.md`
6. 最后再进具体目录
