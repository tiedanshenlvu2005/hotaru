# 2026-07-18 SpaceX 开源分组笔记

来源：GitHub 上 `spacex` 官方公开仓库搜索结果

## 结论先行

SpaceX 官方公开仓库里，`运维` 和 `系统` 类仓库占大头，真正像产品源码的仓库不多。  
如果只挑最值得看的，优先顺序是：

1. `spacex/goawx`
2. `spacex/ControlPlane`
3. `spacex/paperless-ngx`
4. `spacex/helm-paperless-ngx`
5. `spacex/docs`

## AI / 自动化

严格意义上的 AI 仓库很少，更多是自动化和工作流工具：

- `spacex/goawx`
- `spacex/terraform-provider-awx`
- `spacex/puppet-bootstrap`
- `spacex/community.general`
- `spacex/archvsync`

## 运维

- `spacex/helm-charts`
- `spacex/helm-paperless-ngx`
- `spacex/archvsync`
- `spacex/puppet-bootstrap`
- `spacex/puppet-minecraft`
- `spacex/puppet-portage`
- `spacex/community.general`
- `spacex/terraform-namecheap-digitalocean-dns`
- `spacex/terraform-provider-awx`
- `spacex/goawx`

## 系统

- `spacex/kernel-centos5`
- `spacex/kernel-centos6`
- `spacex/kernel-centos7`
- `spacex/ControlPlane`
- `spacex/bbconfig`
- `spacex/cmd2file`

## 文档 / 产品感最强

- `spacex/docs`
- `spacex/paperless-ngx`
- `spacex/helm-paperless-ngx`
- `spacex/mcss`
- `spacex/cmd2file`

## 适合继续深挖的方向

- `goawx`：最像可读的工程实现，适合看自动化/工作流层。
- `ControlPlane`：名字很像核心控制系统，适合研究架构。
- `paperless-ngx`：最像产品源码和可交付应用。
- `docs`：适合看 SpaceX 的文档组织方式。
- `helm-charts`：适合看部署、K8s 和发布组织。

## 一句话记忆

SpaceX 官方公开仓库更像基础设施和自动化工程集合，不是大量产品型开源；如果你只想挑最值得研究的，就先看 `goawx`、`ControlPlane` 和 `paperless-ngx`。

## 仓库速览

### `spacex/goawx`

- 这是 `AWX (Ansible Tower) SDK for the Go programming language`。
- README 里直接说它还是 beta，说明接口和兼容性可能还在变化。
- 适合看 Go 封装外部系统 API 的方式，也适合看自动化调用层的工程写法。

### `spacex/docs`

- 这个仓库的 README 显示它是文档站源码。
- 文档内容是用 `RST` 写的，并通过 `Sphinx` 构建。
- 这类仓库最适合看文档组织、版本化和站点生成流程。

### `spacex/helm-charts`

- README 显示它是 Helm charts 仓库。
- 这类仓库通常更接近部署分发层，而不是业务源码层。
- 适合看服务怎么被打包、部署和参数化。

### `spacex/paperless-ngx`

- README 说明这是一个文档管理系统，把纸质文件变成可检索的线上档案。
- 它有完整的开源产品味道：功能说明、截图、安装方式、贡献说明、演示入口都很完整。
- 从内容密度看，这是这批仓库里最像成品应用的一个。

### `spacex/ControlPlane`

- 这次没有抓到 README，公开信息不足。
- 只按仓库名判断，它更像控制面或管理层系统，偏基础设施方向。
- 后面如果要继续深挖，优先再看目录结构和提交历史，而不是先下结论。
