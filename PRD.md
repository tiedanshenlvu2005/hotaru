# 铁胆神驴 Codex 项目 PRD

## 1. 项目定位

本项目是一个面向 Codex / OpenClaw 工作流的本地整理与执行工作区。它同时承载：

- 工具说明与入口文件
- OpenClaw 相关启动脚本、诊断脚本、修复脚本和运行日志
- 历史输出、备份、说明文档和整理结果
- 面向中文环境和混合编码环境的可读备份

目标不是把它做成单一应用，而是把它整理成一个“能看懂、能查、能控、能追”的工作台。

## 2. 现状问题

### 2.1 文件过多且命名接近

OpenClaw 相关脚本数量多，名称高度相似，容易误点、误用，也容易把诊断、修复、日志混在一起。

### 2.2 入口不统一

当前存在多个入口、多个说明页和多个整理层，第一次进入时不容易判断“从哪里开始看”。

### 2.3 日志、配置、脚本、历史输出混放

同一层里同时出现启动器、日志、说明文本和输出文件，排查问题时会拉高认知成本。

### 2.4 中文编码风险明显

很多文件名和说明是中文，一旦在非 UTF-8 或混合编码环境下查看，就会出现乱码风险。

### 2.5 自动运行风险需要被硬性约束

OpenClaw 和 git 不能被默认自动启动。当前工作区需要明确区分“查看、监控、整理”和“实际执行”。

### 2.6 空目录和历史残留项需要保留边界

部分日期目录目前为空，另外还有少量历史遗留文件名异常项。它们可以保留，但不能让它们污染主入口判断。

## 3. 修复目标

### 3.1 建立单一总入口

用户进入项目后，应优先看到一个明确的总入口和一页式索引，而不是直接面对杂乱文件列表。

### 3.2 建立四类文件分组

OpenClaw 相关文件按用途稳定分为四类：

- `入口`
- `诊断`
- `修复`
- `日志`

### 3.3 建立中英双语可读备份

关键索引、说明和规则文件需要保留英文备份，降低编码和跨环境阅读风险。

### 3.4 建立严格运行边界

默认只允许阅读、整理、监控和分析。任何启动、修复、变更、运行 git 的动作，都必须由用户明确点名。

### 3.5 建立可持续维护方式

后续新增文件必须能按既有分类落位，不再增加新的混乱层。

## 4. 目标用户

- 你本人
- Codex
- 需要接手这个工作区的后续代理

## 5. 核心使用场景

1. 快速查看项目现在是什么、有什么、入口在哪里。
2. 诊断 OpenClaw 当前状态、日志和配置是否异常。
3. 针对问题做受控修复，而不是随手启动一堆脚本。
4. 保留历史输出，便于追溯和复盘。

## 6. 功能需求

### 6.1 目录与入口

- 根目录必须保留一个清晰的项目说明或索引。
- OpenClaw 文件必须分组存放。
- 入口页必须告诉用户先看什么、不要做什么。

### 6.2 诊断能力

- 能读取日志、配置、脚本和历史输出。
- 能复盘失败原因、依赖缺口、命名混乱和编码风险。
- 能把问题写成可执行的修复目标。

### 6.3 修复能力

- 能清理、恢复、限流、重置、切换配置。
- 只能在用户明确授权后执行真正的运行或变更动作。

### 6.4 备份能力

- 中文文件和说明应保留英文备份。
- 关键索引应保留主版本和可读备份版本。

## 7. 非功能需求

- 可读性优先于花哨。
- 稳定性优先于一次性整理。
- 规则优先于临时习惯。
- 追溯性优先于压缩成一个难懂文件。

## 8. 约束

- 不自动启动 OpenClaw。
- 不自动运行 git。
- 不把日志和脚本混在主入口。
- 不删除历史信息，除非用户明确要求。
- 不依赖中文文件名作为唯一入口。

## 9. 验收标准

1. 进入项目后能在 30 秒内知道先看哪里。
2. OpenClaw 文件已经按四类放好。
3. 有中文主版本，也有英文备份。
4. 运行边界写清楚了，且默认禁止自动启动敏感工具。
5. 后续新文件可以沿用同样的分类方法。

## 10. 交付物

- `PRD.md`
- `PRD-en.md`
- `AGENTS.md`
- `AGENTS-en.md`
- `分类索引.md`
- `分类索引-en.md`

