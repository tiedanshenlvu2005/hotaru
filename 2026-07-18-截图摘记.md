# 2026-07-18 截图摘记

来源：

- `屏幕截图 2026-07-18 173549.jpg`
- `屏幕截图 2026-07-18 193617.jpg`
- `屏幕截图 2026-07-18 193655.jpg`
- `屏幕截图 2026-07-18 193813.jpg`
- `屏幕截图 2026-07-18 193831.jpg`

## 1. Grok Build 值得记住的点

- `xai-org/grok-build` 是公开仓库。
- 页面上显示大约 `11.6k stars`、`1.9k forks`、`77 watching`。
- About 里明确写了：`SpaceXAI's coding agent harness and TUI. Fullscreen, mouse interactive, extensible.`
- 中文对应意思就是：SpaceX / xAI 的编程代理工具，支持全屏、鼠标交互、可扩展。
- 目录里能看到 `.cargo/`、`bin/`、`crates/`、`third_party/`、`prod/mc/cli-chat-proxy-types/`，很像 Rust monorepo。
- 画面里还提到它覆盖 `MCP`、子 Agent、`skills`、`plugins`。
- 其中一个画面还写了“几乎全打包了”，说明它不是单一小组件，而是完整工具链样板。

## 2. 对你后续可能有用的判断

- 如果以后要研究 `agent harness`、`TUI`、终端交互式编程工具，这个仓库值得对照。
- 它更像“核心能力外壳 + 可扩展平台”，不是普通演示页。
- 可以重点看：全屏交互、鼠标支持、扩展机制、MCP 接法、sub-agent 组织方式。

## 3. 本地 Ollama 双向聊天页

- 页面目标是让“神驴”和 `Ollama` 互相交流，做能力审计。
- 页面文案里说，这个页面可以把当前 Codex 已启用能力、缺失能力、技能清单交给 Ollama，让它整理对照、缺口和下一步建议。
- 右上角能看到当前模型是 `qwen2.5:latest`，消息数是 `18`。
- 页面里显示过一次 `比对失败`，说明当前对照/连接流程还需要再检查。
- 已启用能力里包含：`Browser`、`Chrome`、`Documents`、`PDF`、`Spreadsheets`、`Presentations`、`Computer-Use`、`Template Creator`、`GitHub`、`Gmail`、`Figma`、`Superpowers`、`Skill Creator`。
- 待确认 / 缺失 / 推荐补充里出现：`Firecrawl`、`Canva`、`HyperFrames`、`Remotion`。

## 4. 可直接复用的页面结构

- 这个 `Ollama` 页面很适合当作“能力审计面板”模板。
- 左侧是对话区，负责输入问题和查看回答。
- 右侧是能力审计区，负责汇总当前已启用能力、缺失能力和推荐补充项。
- 页面上方有模型选择、温度、自动滚动等控制项。
- 常用动作按钮是 `刷新模型` 和 `清空`，很适合做成日常检查入口。
- 这个布局可以直接迁移到你后面要做的“神驴能力体检页”。

## 5. Weather App 参考

- `Build a weather app` 这个提示本身就很适合作为快速原型入口。
- 代码截图里能看到组件名风格，例如 `WeatherApp`、`SkyPlate`、`TickTemp`，说明它在用拆分组件方式做天气页。
- 天气页内容结构很完整，适合当作高密度信息面板参考：
  - 城市名和当前温度
  - `feels like`、高低温
  - 未来小时预报
  - 每日预报
  - `AQI`
  - `precipitation`
  - `UV index`
  - `sunset`
- 如果以后要做天气、资产、风控、健康这类卡片页，这个信息密度和排版方式都能直接借用。

## 6. 一句话结论

这组截图里最值得保留的是两件事：

1. `grok-build` 是一个值得研究的交互式 agent / TUI / MCP / plugin 参考工程。
2. 本地 `Ollama` 页面可以作为“能力审计面板”，专门用来比对当前工具链能力和缺口。
