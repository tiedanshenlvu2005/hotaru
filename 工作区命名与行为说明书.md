# 工作区命名与行为说明书

用途：规定神驴每次启动新任务、创建项目、整理文件、建立页面、生成文档时的命名、编号、沟通和执行规则。

## 总原则

每个任务都要先有名字，再有编号，再有文件夹。

默认工作方法：
1. 先调研。
2. 再写方案。
3. 用户确认。
4. 最后执行。
5. 执行后检查结果。

## 项目编号规则

新项目统一使用：

`PROJECT-序号-短名称`

示例：
- `PROJECT-001-铁胆神驴总入口`
- `PROJECT-002-N1学习资料`
- `PROJECT-003-Web3安全演示`
- `PROJECT-004-桌面整理`
- `PROJECT-005-授权地址整理`

规则：
- `PROJECT` 必须大写。
- 序号使用三位数字，从 `001` 开始。
- 短名称尽量用中文，控制在 4 到 12 个字。
- 不使用特殊符号：`$`, `#`, `%`, `&`, `?`, `*`, `:`, `"`, `<`, `>`, `|`。
- 不在名称里写 API Key、账号、密码、手机号、付款信息。
- 如果是临时任务，在末尾加 `-临时`。
- 如果任务完成，在文件夹内放一个 `完成说明.txt`，不要随便改文件夹名。

## 工作区文件夹规则

推荐结构：

`铁胆神驴Codex`

- `PROJECT-001-铁胆神驴总入口`
- `PROJECT-002-N1学习资料`
- `PROJECT-003-Web3安全演示`
- `PROJECT-004-桌面整理`
- `PROJECT-005-授权地址整理`
- `学习笔记`
- `软件`
- `业务入口`
- `归档`
- `待确认可删除`

规则：
- 重要项目放在 `铁胆神驴Codex` 里面。
- 不确定是否有用的文件，放 `待确认可删除`，不直接删除。
- 已完成但以后可能还要看的项目，放 `归档`。
- 桌面只保留少数入口，不把大量文件散放在桌面。

## 文件命名规则

普通文档：

`日期-主题-用途`

示例：
- `2026-06-07-Codex新手教程-三分钟稿.txt`
- `2026-06-07-OneDrive同步问题-检查记录.md`
- `2026-06-07-N1语法资料-整理清单.xlsx`

页面文件：

- 主入口统一叫 `index.html`
- 中文备用名可以保留，例如 `铁胆神驴总入口.html`
- 启动器统一叫 `打开项目.cmd`

说明文件：

- `打开先看.txt`
- `使用说明.txt`
- `完成说明.txt`
- `检查记录.md`

## 价格和成本规则

涉及付费服务、订阅、API、模型额度、云服务时，统一记录：

- 服务名称
- 官网地址
- 免费额度
- 付费起点
- 是否需要信用卡
- 是否已登录
- 是否已付款
- 风险说明

默认行为：
- 不主动付款。
- 不自动绑定信用卡。
- 不替用户输入密码。
- 不展示完整 API Key。
- 如果费用不确定，先标记为“待确认价格”。

## 每次新任务启动时必须确认

神驴每次启动新任务，先确认下面 6 项：

1. 任务名称是什么？
2. 是否需要创建 `PROJECT-序号-短名称` 文件夹？
3. 素材在哪里？
4. 是否涉及账号、密码、付款、API Key、删除、覆盖？
5. 是否需要先调研和写方案？
6. 完成后用什么方式检查？

如果用户只说“继续”“你来做”，默认采用安全模式：
- 只读检查优先。
- 不删除。
- 不付款。
- 不发布。
- 不覆盖原文件。
- 新文件优先用副本或新名字。

## 分歧记录规则

如果沟通中出现分歧，必须写入：

`全局踩坑日志.md`

必须记录：
- 日期
- 任务
- 分歧点
- 实际确认到什么
- 以后怎么避免

## 当前推荐新项目编号

下一次新项目建议从：

`PROJECT-006-待命名`

开始。


## Local Run Policy

OpenClaw and git do not start on this computer unless the user explicitly asks in the current turn.
Monitoring is always allowed:
- inspect process state
- read logs
- check ports
- verify config or session files

If launch is needed, wait for a direct user instruction before starting anything.
